diff --git a/src/xrt/drivers/wmr/wmr_controller_base.c b/src/xrt/drivers/wmr/wmr_controller_base.c
index f0e0e9e17..f9e9ef612 100644
--- a/src/xrt/drivers/wmr/wmr_controller_base.c
+++ b/src/xrt/drivers/wmr/wmr_controller_base.c
@@ -61,6 +61,7 @@ DEBUG_GET_ONCE_NUM_OPTION(wmr_controller_min_matched_blobs, "WMR_CONTROLLER_MIN_
 DEBUG_GET_ONCE_FLOAT_OPTION(wmr_controller_max_reprojection_error, "WMR_CONTROLLER_MAX_REPROJECTION_ERROR", 35.0f)
 DEBUG_GET_ONCE_FLOAT_OPTION(wmr_controller_max_position_jump, "WMR_CONTROLLER_MAX_POSITION_JUMP", 0.18f)
 DEBUG_GET_ONCE_FLOAT_OPTION(wmr_controller_optical_position_alpha, "WMR_CONTROLLER_OPTICAL_POSITION_ALPHA", 0.25f)
+DEBUG_GET_ONCE_NUM_OPTION(wmr_controller_reacquire_after_rejects, "WMR_CONTROLLER_REACQUIRE_AFTER_REJECTS", 8)
 DEBUG_GET_ONCE_BOOL_OPTION(wmr_controller_task_restart, "WMR_CONTROLLER_TASK_RESTART", false)
 
 
@@ -137,8 +138,11 @@ wmr_controller_base_push_constellation_tracker_sample(struct t_constellation_tra
 	const float max_jump = debug_get_float_option_wmr_controller_max_position_jump();
 	const struct xrt_vec3 sample_position = sample->pose.position;
 
-	bool reject = sample->metrics.matched_blob_count < min_matched ||
-	              sample->metrics.reprojection_error > (double)max_reprojection;
+	const bool quality_reject = sample->metrics.matched_blob_count < min_matched ||
+	                            sample->metrics.reprojection_error > (double)max_reprojection;
+	bool jump_reject = false;
+	bool reacquire = false;
+	bool reject = quality_reject;
 
 	if (!reject && wcb->has_last_constellation_position && max_jump > 0.0f) {
 		const struct xrt_vec3 delta = {
@@ -147,25 +151,37 @@ wmr_controller_base_push_constellation_tracker_sample(struct t_constellation_tra
 		    .z = sample_position.z - wcb->last_constellation_position.z,
 		};
 		const float jump = sqrtf(delta.x * delta.x + delta.y * delta.y + delta.z * delta.z);
-		reject = jump > max_jump;
+		jump_reject = jump > max_jump;
+		reject = jump_reject;
 	}
 
 	if (reject) {
 		wcb->constellation_rejected_sample_count++;
+		if (jump_reject) {
+			wcb->constellation_position_jump_reject_count++;
+			const uint32_t reacquire_after =
+			    (uint32_t)debug_get_num_option_wmr_controller_reacquire_after_rejects();
+			reacquire = reacquire_after > 0 && wcb->constellation_position_jump_reject_count >= reacquire_after;
+		} else {
+			wcb->constellation_position_jump_reject_count = 0;
+		}
 		if (wcb->constellation_rejected_sample_count == 1 ||
-		    wcb->constellation_rejected_sample_count % 60 == 0) {
+		    wcb->constellation_rejected_sample_count % 60 == 0 || reacquire) {
 			WMR_INFO(wcb,
-			         "Rejected WMR optical controller sample #%" PRIu64
+			         "%s WMR optical controller sample #%" PRIu64
 			         " matched=%u reprojection=%.3f pos=(%.3f %.3f %.3f)",
+			         reacquire ? "Reacquiring from" : "Rejected",
 			         wcb->constellation_sample_count, sample->metrics.matched_blob_count,
 			         sample->metrics.reprojection_error, sample_position.x, sample_position.y,
 			         sample_position.z);
 		}
-		return;
+		if (!reacquire) {
+			return;
+		}
 	}
 
 	struct xrt_vec3 filtered_position = sample_position;
-	if (wcb->has_last_constellation_position) {
+	if (wcb->has_last_constellation_position && !reacquire) {
 		float alpha = debug_get_float_option_wmr_controller_optical_position_alpha();
 		alpha = CLAMP(alpha, 0.0f, 1.0f);
 		filtered_position.x = wcb->last_constellation_position.x +
@@ -178,6 +194,7 @@ wmr_controller_base_push_constellation_tracker_sample(struct t_constellation_tra
 
 	wcb->last_constellation_position = filtered_position;
 	wcb->has_last_constellation_position = true;
+	wcb->constellation_position_jump_reject_count = 0;
 
 	struct xrt_space_relation relation = {
 	    .pose.position = filtered_position,
diff --git a/src/xrt/drivers/wmr/wmr_controller_base.h b/src/xrt/drivers/wmr/wmr_controller_base.h
index 2039bc769..5054b0ab8 100644
--- a/src/xrt/drivers/wmr/wmr_controller_base.h
+++ b/src/xrt/drivers/wmr/wmr_controller_base.h
@@ -143,6 +143,7 @@ struct wmr_controller_base
 	bool constellation_registered;
 	uint64_t constellation_sample_count;
 	uint64_t constellation_rejected_sample_count;
+	uint64_t constellation_position_jump_reject_count;
 	bool has_last_constellation_position;
 	struct xrt_vec3 last_constellation_position;
 };
diff --git a/src/xrt/drivers/wmr/wmr_source.c b/src/xrt/drivers/wmr/wmr_source.c
index 32bc2fbeb..b02603cc0 100644
--- a/src/xrt/drivers/wmr/wmr_source.c
+++ b/src/xrt/drivers/wmr/wmr_source.c
@@ -50,8 +50,8 @@ DEBUG_GET_ONCE_NUM_OPTION(wmr_controller_dump_frames, "WMR_CONTROLLER_DUMP_FRAME
 DEBUG_GET_ONCE_NUM_OPTION(wmr_controller_dump_skip_frames, "WMR_CONTROLLER_DUMP_SKIP_FRAMES", 300)
 DEBUG_GET_ONCE_NUM_OPTION(wmr_controller_dump_interval, "WMR_CONTROLLER_DUMP_INTERVAL", 180)
 DEBUG_GET_ONCE_FLOAT_OPTION(wmr_controller_max_bright_fraction, "WMR_CONTROLLER_MAX_BRIGHT_FRACTION", 0.08f)
-DEBUG_GET_ONCE_NUM_OPTION(wmr_controller_min_bright_pixels, "WMR_CONTROLLER_MIN_BRIGHT_PIXELS", 20)
-DEBUG_GET_ONCE_NUM_OPTION(wmr_controller_max_bright_pixels, "WMR_CONTROLLER_MAX_BRIGHT_PIXELS", 5000)
+DEBUG_GET_ONCE_NUM_OPTION(wmr_controller_min_bright_pixels, "WMR_CONTROLLER_MIN_BRIGHT_PIXELS", 0)
+DEBUG_GET_ONCE_NUM_OPTION(wmr_controller_max_bright_pixels, "WMR_CONTROLLER_MAX_BRIGHT_PIXELS", 0)
 
 static void
 controller_frame_stats(struct xrt_frame *xf, uint8_t *out_max, uint32_t *out_above_pixel, uint32_t *out_above_required)
@@ -198,8 +198,9 @@ DEFINE_RECEIVE_CAM(3)
 		const uint32_t max_bright_pixels = (uint32_t)debug_get_num_option_wmr_controller_max_bright_pixels(); \
 		const float max_bright_fraction = debug_get_float_option_wmr_controller_max_bright_fraction();         \
 		const float bright_fraction = (float)above_required / (float)(xf->width * xf->height);                \
-		const bool too_dark = above_required < min_bright_pixels;                                             \
-		const bool too_bright = above_required > max_bright_pixels || bright_fraction > max_bright_fraction;   \
+		const bool too_dark = min_bright_pixels > 0 && above_required < min_bright_pixels;                    \
+		const bool too_bright = (max_bright_pixels > 0 && above_required > max_bright_pixels) ||              \
+		                        bright_fraction > max_bright_fraction;                                        \
 		const bool skip_tracking = too_dark || too_bright;                                                    \
 		if (should_log || may_dump) {                                                                          \
 			if (should_log) {                                                                             \
