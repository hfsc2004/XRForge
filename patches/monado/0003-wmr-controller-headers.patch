diff --git a/src/xrt/drivers/wmr/wmr_common.h b/src/xrt/drivers/wmr/wmr_common.h
index 9e6ac64e1..257683f02 100644
--- a/src/xrt/drivers/wmr/wmr_common.h
+++ b/src/xrt/drivers/wmr/wmr_common.h
@@ -13,6 +13,7 @@
 #pragma once
 
 #include "xrt/xrt_compiler.h"
+#include "xrt/xrt_defines.h"
 
 
 #ifdef __cplusplus
@@ -86,6 +87,12 @@ enum wmr_headset_type
 #define FUJITSU_VID 0x04c5
 #define FMVHDS1_PID 0x15b9
 
+void
+wmr_fallback_update_hmd_position(const struct xrt_space_relation *relation);
+
+bool
+wmr_fallback_get_hmd_position(struct xrt_vec3 *out_position);
+
 /*!
  * @}
  */
diff --git a/src/xrt/drivers/wmr/wmr_controller_base.h b/src/xrt/drivers/wmr/wmr_controller_base.h
index 96fb49f8c..2039bc769 100644
--- a/src/xrt/drivers/wmr/wmr_controller_base.h
+++ b/src/xrt/drivers/wmr/wmr_controller_base.h
@@ -16,6 +16,8 @@
 
 #include "os/os_threading.h"
 #include "math/m_imu_3dof.h"
+#include "math/m_relation_history.h"
+#include "tracking/t_constellation.h"
 #include "util/u_device.h"
 #include "util/u_logging.h"
 #include "xrt/xrt_device.h"
@@ -94,6 +96,9 @@ struct wmr_controller_base
 	//! Base struct.
 	struct xrt_device base;
 
+	//! Receives real 6DoF samples from the constellation tracker.
+	struct t_constellation_tracker_device constellation_device;
+
 	//! Mutex protects the controller connection
 	struct os_mutex conn_lock;
 
@@ -124,6 +129,22 @@ struct wmr_controller_base
 	struct m_imu_3dof fusion;
 	//! The last angular velocity from the IMU, for prediction.
 	struct xrt_vec3 last_angular_velocity;
+
+	//! Camera-derived controller pose history, when available.
+	struct m_relation_history *constellation_relation_history;
+
+	//! Supplies the controller IMU/last-known pose as a prior to the constellation tracker.
+	struct t_constellation_tracker_tracking_source constellation_tracking_source;
+
+	//! LED model handed to the constellation tracker.
+	struct t_constellation_tracker_led *constellation_leds;
+
+	t_constellation_device_id_t constellation_device_id;
+	bool constellation_registered;
+	uint64_t constellation_sample_count;
+	uint64_t constellation_rejected_sample_count;
+	bool has_last_constellation_position;
+	struct xrt_vec3 last_constellation_position;
 };
 
 bool
@@ -136,6 +157,9 @@ wmr_controller_base_init(struct wmr_controller_base *wcb,
 void
 wmr_controller_base_deinit(struct wmr_controller_base *wcb);
 
+struct wmr_controller_base *
+wmr_controller_base_from_xrt_device(struct xrt_device *xdev);
+
 static inline void
 wmr_controller_connection_receive_bytes(struct wmr_controller_connection *wcc,
                                         uint64_t time_ns,
diff --git a/src/xrt/drivers/wmr/wmr_hmd.h b/src/xrt/drivers/wmr/wmr_hmd.h
index e8e5ebc8c..00a2d98a4 100644
--- a/src/xrt/drivers/wmr/wmr_hmd.h
+++ b/src/xrt/drivers/wmr/wmr_hmd.h
@@ -15,6 +15,7 @@
 #pragma once
 
 #include "tracking/t_tracking.h"
+#include "tracking/t_constellation.h"
 #include "xrt/xrt_device.h"
 #include "xrt/xrt_frame.h"
 #include "xrt/xrt_prober.h"
@@ -39,6 +40,8 @@ extern "C" {
 #define WMR_MAX_CONTROLLERS 2
 
 struct wmr_hmd;
+struct t_blobwatch;
+struct t_constellation_tracker;
 
 struct wmr_headset_descriptor
 {
@@ -142,6 +145,24 @@ struct wmr_hmd
 		//! Set at start. Whether the hand tracker was initialized.
 		bool hand_enabled;
 
+		//! Set at start. Whether optical controller tracking was initialized.
+		bool controller_tracking_enabled;
+
+		//! Tracker and sinks for optical WMR controller tracking.
+		struct t_constellation_tracker *controller_tracker;
+		struct t_constellation_tracker_tracking_source controller_tracking_source;
+		struct xrt_slam_sinks controller_sinks;
+		struct t_blobwatch *controller_blobwatches[WMR_MAX_CAMERAS];
+		struct wmr_controller_blob_debug_sink
+		{
+			struct t_blob_sink base;
+			struct t_blob_sink *downstream;
+			struct wmr_hmd *wh;
+			uint32_t camera_index;
+			uint64_t observation_count;
+			uint32_t last_blob_count;
+		} controller_blob_debug_sinks[WMR_MAX_CAMERAS];
+
 		//! SLAM systems track the IMU pose, enabling this corrects it to middle of the eyes
 		bool imu2me;
 	} tracking;
@@ -203,6 +224,9 @@ bool
 wmr_hmd_send_controller_packet(struct wmr_hmd *hmd, const uint8_t *buffer, uint32_t buf_size);
 int
 wmr_hmd_read_sync_from_controller(struct wmr_hmd *hmd, uint8_t *buffer, uint32_t buf_size, int timeout_ms);
+
+bool
+wmr_hmd_register_controller_for_tracking(struct xrt_device *hmd_xdev, struct xrt_device *controller_xdev);
 #ifdef __cplusplus
 }
 #endif
